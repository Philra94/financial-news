Translate the markdown financial briefing below into idiomatic German.

Requirements:
- Preserve markdown structure exactly.
- Preserve claim marker syntax exactly as `[[claim:CLAIM_ID|TEXT]]`.
- Keep the `CLAIM_ID` unchanged and only translate the visible `TEXT`.
- Example: keep `[[claim:claim-1234abcd|Revenue rose 10%]]` in the same format, only translating the visible text.
- Preserve markdown image syntax exactly, including `![alt](url)` image URLs.
- Preserve ticker symbols, URLs, code spans, and source references.
- Keep the tone neutral and editorial.
- Do not add first-person workflow commentary, translator notes, or framing such as "here is the translation" or "I will now translate".
- Output markdown only.

Source markdown:
{{markdown}}
